---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/vitest.config.ts
  - backend/package.json
  - backend/test/setup.ts
  - backend/test/factories/user.factory.ts
  - backend/test/mocks/redis.mock.ts
  - backend/test/mocks/prisma.mock.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - Vitest runs tests and reports coverage
    - Test utilities exist for creating test data
    - Mocks exist for external dependencies (Redis, Prisma)
    - npm run test executes without errors
  artifacts:
    - path: backend/vitest.config.ts
      provides: Vitest configuration with coverage reporting
      exports: ["default"]
    - path: backend/test/setup.ts
      provides: Test environment setup
    - path: backend/test/factories/user.factory.ts
      provides: User test data factory
    - path: backend/test/mocks/redis.mock.ts
      provides: Redis service mock
    - path: backend/test/mocks/prisma.mock.ts
      provides: Prisma client mock
  key_links:
    - from: backend/vitest.config.ts
      to: backend/package.json
      via: test script
      pattern: "vitest run"
---

<objective>
Set up Vitest testing framework with coverage reporting and create test utilities including factories and mocks for external dependencies.

Purpose: Establish the testing infrastructure needed for all subsequent test writing tasks.
Output: Working Vitest configuration, test utilities, and factory functions.
</objective>

<execution_context>
@/Users/kabo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kabo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kabo/Desktop/projects/stripe/.planning/ROADMAP.md
@/Users/kabo/Desktop/projects/stripe/.planning/REQUIREMENTS.md
@/Users/kabo/Desktop/projects/stripe/.planning/codebase/ARCHITECTURE.md
@/Users/kabo/Desktop/projects/stripe/.planning/codebase/CONCERNS.md
@/Users/kabo/Desktop/projects/stripe/backend/package.json

## Current State
- Backend uses Jest (package.json shows jest config)
- No test utilities or factories exist
- No mocks for Redis or Prisma
- Need to migrate to Vitest for better performance and modern features

## Target Configuration
- Vitest with v8 coverage provider
- 80% coverage threshold for lines/functions/branches
- Factories for User entity
- Mocks for RedisService and PrismaService
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and configure testing framework</name>
  <files>backend/package.json, backend/vitest.config.ts</files>
  <action>
Install Vitest and related dependencies, then create vitest.config.ts with coverage reporting.

1. Add devDependencies to package.json:
   - vitest
   - @vitest/coverage-v8
   - unplugin-swc (for NestJS compatibility)

2. Create backend/vitest.config.ts:
   - Use unplugin-swc for TypeScript/decorator support
   - Configure coverage with v8 provider
   - Set thresholds: lines 80, functions 80, branches 80, statements 80
   - Set test environment to node
   - Configure rootDir as src
   - Add setupFiles to load test/setup.ts

3. Update package.json scripts:
   - Change "test" to "vitest run"
   - Change "test:watch" to "vitest"
   - Change "test:cov" to "vitest run --coverage"

4. Remove jest configuration from package.json (keep jest for e2e if needed)

Reference: NestJS + Vitest pattern from TOON TypeScript patterns
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npm install && npm run test -- --run</automated>
  </verify>
  <done>Vitest runs without errors, coverage reporting configured</done>
</task>

<task type="auto">
  <name>Task 2: Create test utilities and factories</name>
  <files>backend/test/setup.ts, backend/test/factories/user.factory.ts, backend/test/mocks/redis.mock.ts, backend/test/mocks/prisma.mock.ts</files>
  <action>
Create test utilities following the factory pattern and mock pattern.

1. Create backend/test/setup.ts:
   - Import vi from vitest
   - Configure global test hooks if needed
   - Set up any global test utilities

2. Create backend/test/factories/user.factory.ts:
   - Export createUserFactory function that returns UserEntity
   - Use faker-js for generating realistic test data
   - Allow overrides via partial object parameter
   - Include all UserEntity fields: id, email, name, password, role, country, emailVerified, createdAt, updatedAt

3. Create backend/test/mocks/redis.mock.ts:
   - Export mockRedisService object with vi.fn() for all methods
   - Methods to mock: get, set, del, getSession, setSession, deleteSession, deletePattern
   - Export createMockRedisService factory function

4. Create backend/test/mocks/prisma.mock.ts:
   - Export mockPrismaService with vi.fn() for user methods
   - Methods: user.create, user.findUnique, user.findFirst, user.update, user.delete
   - Export createMockPrismaService factory function

Follow patterns from ~/.claude/rules/qa-mandatory.md and ~/.claude/rules/testing.md
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npm run test -- --run 2>&1 | head -20</automated>
  </verify>
  <done>Test utilities exist and can be imported without errors</done>
</task>

</tasks>

<verification>
- [ ] Vitest configuration exists at backend/vitest.config.ts
- [ ] npm run test executes successfully
- [ ] Coverage reporting configured with 80% thresholds
- [ ] User factory creates valid UserEntity objects
- [ ] Redis mock has all required methods
- [ ] Prisma mock has all required methods
</verification>

<success_criteria>
1. Vitest runs and reports coverage (TEST-01)
2. Test utilities exist for creating test data (TEST-02)
3. Mocks exist for Redis and Prisma (TEST-02)
4. npm run test passes (no tests yet, but framework works)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
