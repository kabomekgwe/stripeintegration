---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - backend/src/auth/auth.service.spec.ts
  - backend/src/auth/auth.service.ts
autonomous: true
requirements:
  - TEST-03
  - SEC-03
must_haves:
  truths:
    - AuthService has 80%+ test coverage
    - All public methods have unit tests
    - Password reset tokens use crypto.randomBytes
    - Tests verify token generation uses secure random bytes
  artifacts:
    - path: backend/src/auth/auth.service.spec.ts
      provides: AuthService unit tests
      min_lines: 200
    - path: backend/src/auth/auth.service.ts
      provides: Updated with crypto.randomBytes
      contains: "crypto.randomBytes"
  key_links:
    - from: backend/src/auth/auth.service.spec.ts
      to: backend/src/auth/auth.service.ts
      via: import { AuthService }
      pattern: "describe\\(\\s*['\"]AuthService['\"]"
---

<objective>
Write comprehensive unit tests for AuthService achieving 80%+ coverage and replace UUID v4 with crypto.randomBytes for password reset tokens.

Purpose: Ensure authentication logic is tested and secure token generation is used.
Output: AuthService.spec.ts with 80%+ coverage and updated token generation.
</objective>

<execution_context>
@/Users/kabo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kabo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kabo/Desktop/projects/stripe/.planning/phases/01-foundation/01-01-SUMMARY.md
@/Users/kabo/Desktop/projects/stripe/backend/src/auth/auth.service.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/auth/auth.module.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/users/users.service.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/redis/redis.service.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/mail/mail.service.ts

## Current AuthService Methods
- register(registerDto): Creates user, generates token, sends welcome email
- login(loginDto): Validates credentials, generates token
- logout(token): Deletes session from Redis
- validateToken(token): Returns user or null
- requestPasswordReset(dto): Generates reset token using uuidv4 (SECURITY ISSUE)
- resetPassword(dto): Validates token, updates password, invalidates sessions

## Security Issue (SEC-03)
Line 109: const resetToken = uuidv4();
Must be replaced with: crypto.randomBytes(32).toString('hex')

## Dependencies to Mock
- UsersService: create, findByEmail, findById, validatePassword, updatePassword
- JwtService: signAsync
- RedisService: setSession, getSession, deleteSession, set, get, del, deletePattern
- MailService: sendWelcome, sendPasswordReset
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write AuthService unit tests</name>
  <files>backend/src/auth/auth.service.spec.ts</files>
  <behavior>
Test cases to implement:

1. register():
   - Should create user and return user with access token
   - Should throw ConflictException if email exists
   - Should call mailService.sendWelcome
   - Should store session in Redis

2. login():
   - Should return user and token for valid credentials
   - Should throw UnauthorizedException for invalid email
   - Should throw UnauthorizedException for invalid password
   - Should store session in Redis

3. logout():
   - Should delete session from Redis

4. validateToken():
   - Should return user for valid session
   - Should return null for invalid session

5. requestPasswordReset():
   - Should generate reset token and store in Redis
   - Should send password reset email
   - Should not throw if user not found (security)

6. resetPassword():
   - Should update password for valid token
   - Should throw UnauthorizedException for invalid token
   - Should invalidate all sessions
   - Should delete reset token after use

Coverage target: 80%+ lines, functions, branches
</behavior>
  <action>
Create backend/src/auth/auth.service.spec.ts with comprehensive tests.

1. Import required modules: Test from @nestjs/testing, vi from vitest
2. Import AuthService, UsersService, JwtService, RedisService, MailService
3. Import createUserFactory from test/factories/user.factory
4. Import createMockRedisService from test/mocks/redis.mock
5. Create mock implementations for all dependencies using vi.fn()
6. Write test cases for each method following AAA pattern (Arrange, Act, Assert)
7. Ensure all error paths are tested
8. Verify coverage meets 80% threshold

Follow patterns from ~/.claude/rules/testing.md:
- Use beforeEach to reset mocks
- Test happy path and error cases
- Use specific assertions (not toBeTruthy)
- Mock external dependencies
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npm run test -- --run --coverage src/auth/auth.service.spec.ts 2>&1 | grep -E "(Coverage|auth.service)" | head -20</automated>
  </verify>
  <done>AuthService tests pass with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 2: Replace UUID with crypto.randomBytes</name>
  <files>backend/src/auth/auth.service.ts</files>
  <action>
Replace insecure UUID v4 token generation with cryptographically secure random bytes.

1. In backend/src/auth/auth.service.ts:
   - Remove import: import { v4 as uuidv4 } from 'uuid';
   - Add import: import { randomBytes } from 'crypto';
   - Change line 109 from: const resetToken = uuidv4();
   - To: const resetToken = randomBytes(32).toString('hex');

2. Verify the change:
   - Token should be 64 characters (32 bytes hex encoded)
   - Token should be cryptographically random
   - No other changes to logic needed

3. Update tests if needed:
   - Tests should verify token is generated
   - Tests should not depend on specific token format
   - Ensure resetPassword test still works with new token format
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && grep -n "randomBytes" src/auth/auth.service.ts && npm run test -- --run src/auth/auth.service.spec.ts</automated>
  </verify>
  <done>AuthService uses crypto.randomBytes for reset tokens, all tests pass</done>
</task>

</tasks>

<verification>
- [ ] AuthService.spec.ts exists with comprehensive tests
- [ ] Test coverage is 80%+ for lines, functions, branches
- [ ] All public methods have tests
- [ ] Error cases are tested
- [ ] UUID import removed from auth.service.ts
- [ ] crypto.randomBytes used for token generation
- [ ] Tests pass after token generation change
</verification>

<success_criteria>
1. AuthService has 80%+ test coverage (TEST-03)
2. Password reset uses crypto.randomBytes (SEC-03)
3. All tests pass
4. No UUID v4 used for security tokens
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
