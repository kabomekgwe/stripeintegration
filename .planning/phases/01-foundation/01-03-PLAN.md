---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
files_modified:
  - backend/src/common/guards/rate-limit.guard.ts
  - backend/src/common/middleware/global-rate-limit.middleware.ts
  - backend/src/app.module.ts
  - backend/src/health/health.controller.ts
  - backend/src/health/health.module.ts
  - backend/src/health/health.service.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - OBS-01
  - OBS-02
must_haves:
  truths:
    - Rate limiting middleware blocks excessive requests
    - Auth endpoints limited to 5 requests per minute
    - Payment endpoints limited to 10 requests per minute
    - /health returns 200 when service is alive
    - /health/ready returns 200 when DB and Redis are connected
    - /health/ready returns 503 when dependencies are down
  artifacts:
    - path: backend/src/common/middleware/global-rate-limit.middleware.ts
      provides: Global rate limiting
      exports: ["GlobalRateLimitMiddleware"]
    - path: backend/src/common/guards/rate-limit.guard.ts
      provides: Per-route rate limiting
      exports: ["RateLimitGuard"]
    - path: backend/src/health/health.controller.ts
      provides: Health check endpoints
      exports: ["HealthController"]
    - path: backend/src/health/health.service.ts
      provides: Health check logic
      exports: ["HealthService"]
  key_links:
    - from: backend/src/app.module.ts
      to: backend/src/common/middleware/global-rate-limit.middleware.ts
      via: configure(consumer)
      pattern: "apply\\(.*RateLimit"
    - from: backend/src/health/health.controller.ts
      to: backend/src/health/health.service.ts
      via: constructor injection
      pattern: "HealthService"
---

<objective>
Implement rate limiting middleware (global and per-route) and create health check endpoints for liveness and readiness probes.

Purpose: Protect API from abuse and provide monitoring endpoints for production deployment.
Output: Rate limiting guards/middleware and health check endpoints.
</objective>

<execution_context>
@/Users/kabo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kabo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kabo/Desktop/projects/stripe/.planning/phases/01-foundation/01-01-SUMMARY.md
@/Users/kabo/Desktop/projects/stripe/.planning/phases/01-foundation/01-02-SUMMARY.md
@/Users/kabo/Desktop/projects/stripe/backend/src/app.module.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/main.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/redis/redis.service.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/database/prisma.service.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/payments/guards/rate-limit.guard.ts

## Existing Rate Limiting
- backend/src/payments/guards/rate-limit.guard.ts exists
- Uses custom implementation with Redis
- Pattern can be extended for global use

## Rate Limiting Requirements (SEC-01, SEC-02)
- Global: 100 requests per minute per IP
- Auth endpoints (/auth/*): 5 requests per minute
- Payment endpoints (/payments/*): 10 requests per minute
- Store limits in Redis with TTL

## Health Check Requirements (OBS-01, OBS-02)
- GET /health: Simple liveness check, returns 200 { status: "ok" }
- GET /health/ready: Readiness check with DB and Redis verification
  - Returns 200 if both DB and Redis are connected
  - Returns 503 if either is down
  - Response includes: status, timestamp, checks: { database, redis }
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create global rate limiting middleware</name>
  <files>backend/src/common/middleware/global-rate-limit.middleware.ts, backend/src/common/guards/rate-limit.guard.ts, backend/src/app.module.ts</files>
  <action>
Create global rate limiting middleware and update existing rate limit guard.

1. Create backend/src/common/middleware/global-rate-limit.middleware.ts:
   - Implement NestMiddleware interface
   - Inject RedisService
   - Use IP address as key (req.ip)
   - Limit: 100 requests per minute per IP
   - Return 429 status with Retry-After header when limit exceeded
   - Use Redis INCR with 60s TTL for tracking

2. Update backend/src/common/guards/rate-limit.guard.ts (or create if needed):
   - Implement CanActivate interface
   - Support configurable limits per route
   - Use @SetMetadata('rateLimit', { limit: number, windowMs: number }) decorator
   - Default: 100 req/min, Auth: 5 req/min, Payments: 10 req/min
   - Store in Redis with key pattern: rate_limit:{route}:{ip}

3. Update backend/src/app.module.ts:
   - Import GlobalRateLimitMiddleware
   - Apply to all routes in configure(consumer) method
   - Exclude /health and /health/ready from rate limiting

4. Create backend/src/common/decorators/rate-limit.decorator.ts:
   - Export RateLimit decorator using SetMetadata
   - Usage: @RateLimit(5, 60000) for 5 requests per minute

Follow patterns from ~/.claude/rules/security.md for rate limiting
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npm run test -- --run 2>&1 | head -10</automated>
  </verify>
  <done>Rate limiting middleware and guard created, applied in AppModule</done>
</task>

<task type="auto">
  <name>Task 2: Apply per-route rate limiting</name>
  <files>backend/src/auth/auth.controller.ts, backend/src/payments/payments.controller.ts</files>
  <action>
Apply specific rate limits to auth and payment endpoints.

1. Update backend/src/auth/auth.controller.ts:
   - Import RateLimit decorator
   - Apply @RateLimit(5, 60000) to login, register, forgot-password endpoints
   - 5 requests per minute for auth endpoints

2. Update backend/src/payments/payments.controller.ts:
   - Import RateLimit decorator
   - Apply @RateLimit(10, 60000) to createPaymentIntent, createRefund endpoints
   - 10 requests per minute for payment endpoints

3. Ensure RateLimitGuard is registered globally or in each module
   - Add to AuthModule providers if not global
   - Add to PaymentsModule providers if not global

4. Test rate limiting:
   - Verify 429 response when limit exceeded
   - Verify Retry-After header present
   - Verify different limits apply to different routes
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && grep -r "RateLimit" src/auth src/payments --include="*.ts" | head -10</automated>
  </verify>
  <done>Auth endpoints limited to 5/min, payment endpoints limited to 10/min</done>
</task>

<task type="auto">
  <name>Task 3: Create health check module and endpoints</name>
  <files>backend/src/health/health.module.ts, backend/src/health/health.controller.ts, backend/src/health/health.service.ts, backend/src/app.module.ts</files>
  <action>
Create health check module with liveness and readiness endpoints.

1. Create backend/src/health/health.module.ts:
   - Import Module from @nestjs/common
   - Import HealthController and HealthService
   - Import PrismaService and RedisService
   - Export HealthService

2. Create backend/src/health/health.service.ts:
   - Inject PrismaService and RedisService
   - Implement checkLiveness(): returns { status: "ok", timestamp }
   - Implement checkReadiness(): checks DB and Redis connections
     - Try PrismaService.$queryRaw`SELECT 1`
     - Try RedisService.ping() or get()
     - Return { status: "healthy" | "unhealthy", timestamp, checks: { database: "up" | "down", redis: "up" | "down" } }

3. Create backend/src/health/health.controller.ts:
   - @Controller('health')
   - @Get() checkLiveness(): returns 200 with status ok
   - @Get('ready') checkReadiness(): returns 200 if healthy, 503 if unhealthy
   - Use HealthService for logic

4. Update backend/src/app.module.ts:
   - Import HealthModule
   - Add to imports array

5. Follow patterns from ~/.claude/rules/reliability.md for health checks
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Health module created with /health and /health/ready endpoints</done>
</task>

<task type="auto">
  <name>Task 4: Write tests for rate limiting and health endpoints</name>
  <files>backend/src/common/guards/rate-limit.guard.spec.ts, backend/src/health/health.controller.spec.ts, backend/src/health/health.service.spec.ts</files>
  <action>
Write unit tests for rate limiting and health check functionality.

1. Create backend/src/common/guards/rate-limit.guard.spec.ts:
   - Test that guard allows requests under limit
   - Test that guard blocks requests over limit (returns false)
   - Test different limits for different routes
   - Mock RedisService

2. Create backend/src/health/health.service.spec.ts:
   - Test checkLiveness returns ok status
   - Test checkReadiness returns healthy when DB and Redis are up
   - Test checkReadiness returns unhealthy when DB is down
   - Test checkReadiness returns unhealthy when Redis is down
   - Mock PrismaService and RedisService

3. Create backend/src/health/health.controller.spec.ts:
   - Test GET /health returns 200 with ok status
   - Test GET /health/ready returns 200 when healthy
   - Test GET /health/ready returns 503 when unhealthy
   - Mock HealthService

4. Run all tests and verify coverage
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npm run test -- --run --coverage 2>&1 | grep -E "(PASS|FAIL|Coverage)" | head -20</automated>
  </verify>
  <done>All rate limiting and health tests pass</done>
</task>

</tasks>

<verification>
- [ ] Global rate limiting middleware exists and is applied
- [ ] RateLimitGuard exists with configurable limits
- [ ] Auth endpoints have 5 req/min limit
- [ ] Payment endpoints have 10 req/min limit
- [ ] HealthController exists with /health endpoint
- [ ] /health returns 200 { status: "ok" }
- [ ] /health/ready returns 200 when dependencies are up
- [ ] /health/ready returns 503 when dependencies are down
- [ ] Tests exist for rate limiting and health checks
</verification>

<success_criteria>
1. Rate limiting blocks excessive requests (SEC-01)
2. Per-route rate limiting configured for auth (5/min) and payments (10/min) (SEC-02)
3. /health endpoint returns 200 for liveness (OBS-01)
4. /health/ready endpoint returns 200/503 appropriately (OBS-02)
5. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
