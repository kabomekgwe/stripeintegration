---
phase: 03-webhooks-performance
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - backend/src/connect/connect.service.ts
  - backend/src/promo-codes/promo-code.service.ts
  - backend/src/disputes/dispute.service.ts
  - backend/src/mail/mail.service.ts
autonomous: true
requirements:
  - TYPE-05
  - TYPE-06
  - TYPE-07
  - TYPE-08
must_haves:
  truths:
    - ConnectService has no 'any' types
    - PromoCodeService has no 'any' types
    - DisputeService has no 'any' types
    - MailService has no 'any' types and null assertions are fixed
    - TypeScript compilation passes with strict mode
  artifacts:
    - path: backend/src/connect/connect.service.ts
      provides: Type-safe ConnectService
      pattern: "account:.*Stripe.Account"
    - path: backend/src/promo-codes/promo-code.service.ts
      provides: Type-safe PromoCodeService
      pattern: "promotionCode:.*Stripe.PromotionCode"
    - path: backend/src/disputes/dispute.service.ts
      provides: Type-safe DisputeService
      pattern: "dispute:.*Stripe.Dispute"
    - path: backend/src/mail/mail.service.ts
      provides: Type-safe MailService
      pattern: "transporter.*SendMailOptions"
  key_links:
    - from: connect.service.ts
      to: Stripe SDK types
      via: Stripe.Account, Stripe.Transfer
      pattern: "stripe\\.accounts\\.create"
---

<objective>
Replace all remaining 'any' types in ConnectService, PromoCodeService, DisputeService, and fix null assertions in MailService.

Purpose: Complete type safety across all remaining services by replacing 'any' with proper Stripe SDK types and fixing unsafe null assertions.

Output: Type-safe services with no 'any' types and proper null handling.
</objective>

<execution_context>
@/Users/kabo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kabo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/Users/kabo/Desktop/projects/stripe/backend/src/connect/connect.service.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/promo-codes/promo-code.service.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/disputes/dispute.service.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/mail/mail.service.ts

## Services Requiring Type Fixes

### ConnectService (TYPE-05)
File: backend/src/connect/connect.service.ts

Current 'any' types found:
- Line 27: `Promise<{ account: any; onboardingUrl?: string }>` - return type uses any
- Various Stripe API responses may be typed as any

Required types from Stripe SDK:
- Stripe.Account for connected account
- Stripe.AccountLink for onboarding URL
- Stripe.Transfer for transfers
- Stripe.LoginLink for dashboard access

### PromoCodeService (TYPE-06)
File: backend/src/promo-codes/promo-code.service.ts

Current 'any' types found:
- Stripe PromotionCode and Coupon types may use any

Required types from Stripe SDK:
- Stripe.PromotionCode for promo codes
- Stripe.Coupon for coupons
- Stripe.DeletedPromotionCode for deleted status

### DisputeService (TYPE-07)
File: backend/src/disputes/dispute.service.ts

Current 'any' types found:
- Dispute handling may use any for dispute data

Required types from Stripe SDK:
- Stripe.Dispute for disputes
- Stripe.DisputeEvidence for evidence submission

### MailService (TYPE-08)
File: backend/src/mail/mail.service.ts

Current issues:
- Null assertions (e.g., `transporter!`) should be replaced with proper null checks
- Mail options may use any or Partial types

Required types:
- nodemailer.SendMailOptions for mail options
- Proper null handling for transporter
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 'any' types in ConnectService</name>
  <files>backend/src/connect/connect.service.ts</files>
  <action>
    Fix 'any' types in ConnectService:

    1. Import proper Stripe types at the top of the file:
       ```typescript
       import Stripe from 'stripe';
       ```

    2. Replace return type on line 27:
       From: `Promise<{ account: any; onboardingUrl?: string }>`
       To: `Promise<{ account: Stripe.Account; onboardingUrl?: string }>`

    3. Review all method return types and ensure they use proper Stripe types:
       - createConnectedAccount: returns Stripe.Account
       - getConnectedAccount: returns Stripe.Account | null
       - createTransfer: returns Stripe.Transfer
       - getAccountBalance: returns Stripe.Balance
       - getPayouts: returns Stripe.Payout[]
       - createDashboardLink: returns Stripe.LoginLink

    4. Check method parameters for any 'any' types and replace with:
       - accountId: string (already correct)
       - transferData: CreateTransferDto (already correct)

    5. Ensure all Stripe API responses are properly typed by using the generic type parameter:
       ```typescript
       const account = await this.stripe.accounts.create(params) as Stripe.Account;
       ```

    6. Run TypeScript compiler to verify no 'any' types remain.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit 2>&1 | grep -i "connect.service" | grep -i "any" && echo "FAIL: any types found" || echo "PASS: No any types"</automated>
  </verify>
  <done>ConnectService has no 'any' types, all Stripe types properly imported and used</done>
</task>

<task type="auto">
  <name>Task 2: Replace 'any' types in PromoCodeService and DisputeService</name>
  <files>backend/src/promo-codes/promo-code.service.ts, backend/src/disputes/dispute.service.ts</files>
  <action>
    Fix 'any' types in PromoCodeService and DisputeService:

    PromoCodeService:
    1. Import Stripe types: `import Stripe from 'stripe'`
    2. Replace any return types:
       - createPromoCode: returns Stripe.PromotionCode
       - getPromoCode: returns Stripe.PromotionCode | null
       - updatePromoCode: returns Stripe.PromotionCode
       - deletePromoCode: returns Stripe.DeletedPromotionCode
       - validatePromoCode: returns { valid: boolean; discount?: Stripe.Coupon }
    3. Check for any 'any' in parameters and replace with proper DTOs or Stripe types

    DisputeService:
    1. Import Stripe types: `import Stripe from 'stripe'`
    2. Replace any return types:
       - handleDisputeCreated: parameter should be Stripe.Dispute
       - handleDisputeUpdated: parameter should be Stripe.Dispute
       - submitEvidence: returns Stripe.Dispute
       - closeDispute: returns Stripe.Dispute
    3. Ensure webhook handlers in webhooks.service.ts pass properly typed Stripe.Dispute objects

    4. Check for any internal 'any' types in method implementations and replace with:
       - Dispute evidence: Stripe.DisputeEvidence
       - Dispute status: Stripe.Dispute.Status

    5. Run TypeScript compiler to verify no 'any' types remain in either service.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit 2>&1 | grep -E "(promo-code|dispute)" | grep -i "any" && echo "FAIL: any types found" || echo "PASS: No any types"</automated>
  </verify>
  <done>PromoCodeService and DisputeService have no 'any' types, all Stripe types properly used</done>
</task>

<task type="auto">
  <name>Task 3: Fix null assertions in MailService</name>
  <files>backend/src/mail/mail.service.ts</files>
  <action>
    Fix unsafe null assertions in MailService:

    1. Review the MailService for null assertion patterns like `transporter!` or `value!`

    2. Replace null assertions with proper null checks:
       ```typescript
       // Before (unsafe):
       await this.transporter!.sendMail(options);

       // After (safe):
       if (!this.transporter) {
         throw new Error('Mail transporter not initialized');
       }
       await this.transporter.sendMail(options);
       ```

    3. Ensure proper typing for mail options:
       ```typescript
       import { SendMailOptions } from 'nodemailer';

       async sendMail(options: SendMailOptions): Promise<void> {
         // implementation
       }
       ```

    4. Check constructor and initialization:
       - Ensure transporter is properly typed as `Transporter | null` or `Transporter | undefined`
       - Initialize in constructor or onModuleInit with proper error handling

    5. Review all mail sending methods:
       - sendVerificationEmail
       - sendPasswordResetEmail
       - sendPaymentReceipt
       - sendInvoiceEmail
       - sendDisputeNotification

    6. Add proper error handling when transporter is not available:
       ```typescript
       if (!this.transporter) {
         this.logger.error('Mail service not configured');
         throw new ServiceUnavailableException('Email service unavailable');
       }
       ```

    7. Run TypeScript compiler with strict null checks to verify fixes.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit --strictNullChecks 2>&1 | grep -i "mail.service" | grep -E "(!\.|TS[0-9])" && echo "FAIL: null assertion issues found" || echo "PASS: No null assertion issues"</automated>
  </verify>
  <done>MailService has no unsafe null assertions, proper null checks implemented, all mail methods type-safe</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes with strict mode: `cd backend && npx tsc --noEmit`
2. No 'any' types in target services:
   ```bash
   cd backend && grep -n "any" src/connect/connect.service.ts src/promo-codes/promo-code.service.ts src/disputes/dispute.service.ts | grep -v "// " | head -20
   ```
   Should return empty or only acceptable any types (if any exist in comments)
3. No null assertions in MailService:
   ```bash
   cd backend && grep -n "!\." src/mail/mail.service.ts | grep -v "// "
   ```
   Should return empty (no non-null assertions)
4. All services follow existing patterns from PaymentsService and SubscriptionService
</verification>

<success_criteria>
- ConnectService has no 'any' types, uses proper Stripe.Account, Stripe.Transfer, etc.
- PromoCodeService has no 'any' types, uses proper Stripe.PromotionCode, Stripe.Coupon
- DisputeService has no 'any' types, uses proper Stripe.Dispute
- MailService has no unsafe null assertions (!.), proper null checks in place
- TypeScript compilation passes with strict mode enabled
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-webhooks-performance/03-03-SUMMARY.md`
</output>
