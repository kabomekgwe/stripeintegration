---
phase: 02-core-services
plan: 02-01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/payments/payments.service.spec.ts
  - backend/test/factories/payment.factory.ts
  - backend/test/factories/invoice.factory.ts
autonomous: true
requirements:
  - TEST-04
must_haves:
  truths:
    - PaymentsService has 80%+ test coverage
    - All public methods are tested
    - Edge cases and error scenarios covered
  artifacts:
    - path: backend/src/payments/payments.service.spec.ts
      provides: Comprehensive PaymentsService tests
      exports: ["describe", "it", "beforeEach"]
    - path: backend/test/factories/payment.factory.ts
      provides: Payment entity factory
    - path: backend/test/factories/invoice.factory.ts
      provides: Invoice entity factory
  key_links:
    - from: backend/src/payments/payments.service.spec.ts
      to: backend/src/payments/payments.service.ts
      via: test coverage
---

<objective>
Write comprehensive unit tests for PaymentsService achieving 80%+ coverage.

Purpose: Ensure payment processing logic is reliable and correctly handles various scenarios including successful payments, failures, refunds, and edge cases.
Output: Complete test suite for PaymentsService with factories for test data.
</objective>

<execution_context>
@/Users/kabo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kabo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kabo/Desktop/projects/stripe/.planning/ROADMAP.md
@/Users/kabo/Desktop/projects/stripe/.planning/REQUIREMENTS.md
@/Users/kabo/Desktop/projects/stripe/backend/src/payments/payments.service.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/payments/payments.module.ts

## Current State
- PaymentsService exists but has no tests
- Need to create factories for Payment and Invoice entities
- Testing infrastructure from Phase 01 is ready (Vitest, factories, mocks)

## Target Coverage
- 80%+ line coverage
- 80%+ function coverage
- 80%+ branch coverage
- All public methods tested
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create payment and invoice factories</name>
  <files>backend/test/factories/payment.factory.ts, backend/test/factories/invoice.factory.ts</files>
  <action>
Create factory functions for Payment and Invoice entities following the existing user.factory.ts pattern.

1. Create backend/test/factories/payment.factory.ts:
   - Export createPaymentFactory function
   - Use faker-js for generating realistic test data
   - Include all Payment entity fields: id, userId, amount, currency, status, stripePaymentIntentId, createdAt, updatedAt
   - Allow overrides via partial object parameter

2. Create backend/test/factories/invoice.factory.ts:
   - Export createInvoiceFactory function
   - Include all Invoice entity fields: id, userId, amount, currency, status, stripeInvoiceId, paidAt, createdAt, updatedAt
   - Allow overrides via partial object parameter

Reference existing backend/test/factories/user.factory.ts for patterns.
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npm run test -- --run test/factories/payment.factory.ts test/factories/invoice.factory.ts</automated>
  </verify>
  <done>Factories can create valid Payment and Invoice entities</done>
</task>

<task type="auto">
  <name>Task 2: Write PaymentsService tests - core methods</name>
  <files>backend/src/payments/payments.service.spec.ts</files>
  <action>
Create comprehensive tests for PaymentsService core functionality.

1. Create backend/src/payments/payments.service.spec.ts:
   - Import necessary dependencies (Vitest, factories, mocks)
   - Set up test module with mocked dependencies (Prisma, Stripe, Redis)

2. Test createPaymentIntent:
   - Success case: creates payment intent with valid data
   - Error case: throws when user not found
   - Error case: handles Stripe API errors
   - Verify correct amount and currency are passed to Stripe

3. Test confirmPayment:
   - Success case: confirms payment and updates database
   - Error case: handles invalid payment intent
   - Error case: handles already confirmed payments
   - Verify invoice is created on successful payment

4. Test getPaymentStatus:
   - Returns correct status for existing payment
   - Throws error for non-existent payment
   - Handles Stripe API lookup correctly

5. Test refundPayment:
   - Success case: processes refund and updates status
   - Error case: throws for non-refundable payments
   - Error case: handles Stripe refund failures
   - Verify refund amount cannot exceed original payment

Use the existing test patterns from auth.service.spec.ts as reference.
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npm run test -- --run --coverage src/payments/payments.service.spec.ts</automated>
  </verify>
  <done>Core payment methods have tests with 80%+ coverage</done>
</task>

<task type="auto">
  <name>Task 3: Write PaymentsService tests - edge cases and error handling</name>
  <files>backend/src/payments/payments.service.spec.ts</files>
  <action>
Add edge case and error handling tests to achieve 80%+ branch coverage.

1. Test currency handling:
   - Multiple currency support (USD, EUR, GBP)
   - Invalid currency rejection
   - Currency conversion edge cases

2. Test amount validation:
   - Zero amount rejection
   - Negative amount handling
   - Very large amount handling
   - Decimal precision handling

3. Test concurrent operations:
   - Multiple simultaneous payment attempts
   - Race condition handling
   - Idempotency key usage

4. Test webhook handling:
   - Payment succeeded webhook
   - Payment failed webhook
   - Payment refunded webhook
   - Invalid webhook signature handling

5. Test database transaction rollback:
   - Verify rollback on Stripe API failure
   - Verify rollback on database error
   - Ensure data consistency in error scenarios

Run coverage report and verify all branches are covered.
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npm run test -- --run --coverage src/payments/payments.service.spec.ts 2>&1 | grep -A 10 "Coverage summary"</automated>
  </verify>
  <done>All edge cases covered, 80%+ branch coverage achieved</done>
</task>

</tasks>

<verification>
- [ ] Payment factory creates valid Payment entities
- [ ] Invoice factory creates valid Invoice entities
- [ ] createPaymentIntent has tests for success and error cases
- [ ] confirmPayment has tests for success and error cases
- [ ] getPaymentStatus returns correct status
- [ ] refundPayment handles refunds correctly
- [ ] Edge cases covered (currencies, amounts, concurrent ops)
- [ ] Coverage report shows 80%+ for lines, functions, branches
</verification>

<success_criteria>
1. PaymentsService has 80%+ test coverage (TEST-04)
2. All public methods have comprehensive tests
3. Edge cases and error scenarios are covered
4. Factories exist for Payment and Invoice entities
5. Tests run successfully with npm run test
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-services/02-01-SUMMARY.md`
</output>
