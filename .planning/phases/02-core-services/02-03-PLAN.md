---
phase: 02-core-services
plan: 02-03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - backend/src/payments/payments.service.ts
  - backend/src/subscriptions/subscriptions.service.ts
  - backend/src/users/users.service.ts
  - backend/src/users/dto/suspend-user.dto.ts
  - backend/src/auth/auth.service.ts
autonomous: true
requirements:
  - TYPE-03
  - TYPE-04
  - BUG-01
must_haves:
  truths:
    - No 'any' types in payments service
    - No 'any' types in subscriptions service
    - User suspension logic is implemented and tested
  artifacts:
    - path: backend/src/payments/payments.service.ts
      provides: Type-safe payment service
    - path: backend/src/subscriptions/subscriptions.service.ts
      provides: Type-safe subscription service
    - path: backend/src/users/users.service.ts
      provides: User suspension functionality
    - path: backend/src/users/dto/suspend-user.dto.ts
      provides: User suspension DTO
  key_links:
    - from: backend/src/users/users.service.ts
      to: backend/src/auth/auth.service.ts
      via: suspension check during login
---

<objective>
Fix remaining type safety issues by replacing all 'any' types in payments and subscriptions services, and implement user suspension logic.

Purpose: Achieve full type safety in core services and add user suspension capability for security/compliance.
Output: Type-safe services with no 'any' types and working user suspension feature.
</objective>

<execution_context>
@/Users/kabo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kabo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kabo/Desktop/projects/stripe/.planning/ROADMAP.md
@/Users/kabo/Desktop/projects/stripe/.planning/REQUIREMENTS.md
@/Users/kabo/Desktop/projects/stripe/backend/src/payments/payments.service.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/subscriptions/subscriptions.service.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/users/users.service.ts

## Current State
- PaymentsService may have 'any' types in some places
- SubscriptionsService may have 'any' types
- User suspension logic is not implemented (BUG-01)
- Need to ensure type safety across all core services
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 'any' types in PaymentsService</name>
  <files>backend/src/payments/payments.service.ts</files>
  <action>
Find and replace all 'any' types in PaymentsService with proper types.

1. Search for 'any' types in backend/src/payments/payments.service.ts:
   - grep -n "any" backend/src/payments/payments.service.ts

2. Replace with proper types:
   - Stripe API responses: Use Stripe.PaymentIntent, Stripe.Invoice, etc.
   - Function parameters: Define proper interfaces
   - Return types: Ensure all functions have explicit return types
   - Generic types: Use unknown with type guards where appropriate

3. Common replacements:
   - any Stripe response -> Stripe.PaymentIntent | Stripe.Invoice | etc.
   - any payment data -> CreatePaymentDto or PaymentEntity
   - any metadata -> Record<string, string> or specific interface

4. Import types from Stripe SDK:
   - import Stripe from 'stripe'
   - Use Stripe namespace types (Stripe.PaymentIntent, etc.)

5. Run TypeScript compiler to verify:
   - npx tsc --noEmit
   - Fix any type errors
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npx tsc --noEmit src/payments/payments.service.ts && grep -c "any" src/payments/payments.service.ts || echo "0 any types found"</automated>
  </verify>
  <done>PaymentsService has no 'any' types and compiles successfully</done>
</task>

<task type="auto">
  <name>Task 2: Replace 'any' types in SubscriptionsService</name>
  <files>backend/src/subscriptions/subscriptions.service.ts</files>
  <action>
Find and replace all 'any' types in SubscriptionsService with proper types.

1. Search for 'any' types:
   - grep -n "any" backend/src/subscriptions/subscriptions.service.ts

2. Replace with proper types:
   - Stripe subscription responses -> Stripe.Subscription
   - Plan/price data -> Stripe.Price, Stripe.Product
   - Subscription status -> Use enum or union type
   - Any metadata -> Record<string, string>

3. Define missing types:
   - CreateSubscriptionInput interface
   - UpdateSubscriptionInput interface
   - SubscriptionStatus enum if not exists

4. Import Stripe types:
   - Ensure Stripe namespace types are imported
   - Use proper generic types for API responses

5. Run TypeScript compiler:
   - npx tsc --noEmit
   - Fix any type errors
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npx tsc --noEmit src/subscriptions/subscriptions.service.ts && grep -c "any" src/subscriptions/subscriptions.service.ts || echo "0 any types found"</automated>
  </verify>
  <done>SubscriptionsService has no 'any' types and compiles successfully</done>
</task>

<task type="auto">
  <name>Task 3: Implement user suspension logic</name>
  <files>backend/src/users/dto/suspend-user.dto.ts, backend/src/users/users.service.ts, backend/src/auth/auth.service.ts</files>
  <action>
Implement user suspension feature to prevent suspended users from accessing the system.

1. Create backend/src/users/dto/suspend-user.dto.ts:
   - SuspendUserDto with reason: string, duration?: number (days)
   - UnsuspendUserDto with reason: string
   - Validation decorators from class-validator

2. Update backend/src/users/users.service.ts:
   - Add suspendUser(userId: string, dto: SuspendUserDto) method
     * Set user.suspended = true
     * Set user.suspendedAt = new Date()
     * Set user.suspensionReason = dto.reason
     * Set user.suspensionExpiry if duration provided
     * Revoke all active sessions (call auth service)
   - Add unsuspendUser(userId: string, dto: UnsuspendUserDto) method
     * Set user.suspended = false
     * Clear suspension fields
     * Log the unsuspension action
   - Add isUserSuspended(userId: string) method
     * Check suspended flag
     * Check if suspension has expired
     * Return boolean
   - Add getSuspendedUsers() method for admin use

3. Update backend/src/auth/auth.service.ts:
   - Modify login method to check if user is suspended
   - Throw UnauthorizedException with message "Account suspended" if suspended
   - Add check in validateToken to reject suspended users
   - Add revokeAllUserSessions(userId: string) method

4. Update UserEntity (if needed):
   - Add suspended: boolean field
   - Add suspendedAt: Date field
   - Add suspensionReason: string field
   - Add suspensionExpiry: Date | null field

5. Create database migration if adding new fields:
   - npx prisma migrate dev --name add_user_suspension

6. Write tests for suspension logic:
   - Test suspendUser prevents login
   - Test unsuspendUser allows login again
   - Test automatic unsuspension after expiry
   - Test admin can view suspended users
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npm run test -- --run src/users/users.service.spec.ts src/auth/auth.service.spec.ts</automated>
  </verify>
  <done>User suspension is implemented and tested</done>
</task>

<task type="auto">
  <name>Task 4: Verify type safety across all services</name>
  <files>backend/src/**/*.ts</files>
  <action>
Run comprehensive type check across all services to ensure no 'any' types remain.

1. Run TypeScript compiler on entire project:
   - cd backend && npx tsc --noEmit

2. Check for any remaining 'any' types:
   - grep -r ": any" src/ --include="*.ts" | grep -v node_modules | grep -v ".spec.ts"
   - Should return empty or only in test files

3. Check for implicit any:
   - Review tsconfig.json strict settings
   - Ensure noImplicitAny is enabled

4. Run linter to catch type issues:
   - npm run lint

5. Fix any remaining issues:
   - Add explicit types to all function parameters
   - Add return types to all functions
   - Replace any with unknown + type guards where needed

6. Document any intentional 'any' usage:
   - Add comments explaining why 'any' is necessary
   - Use eslint-disable with explanation if truly needed
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npx tsc --noEmit && echo "Type check passed"</automated>
  </verify>
  <done>All services are type-safe with no 'any' types</done>
</task>

</tasks>

<verification>
- [ ] PaymentsService has no 'any' types
- [ ] SubscriptionsService has no 'any' types
- [ ] User suspension DTOs created
- [ ] suspendUser method implemented
- [ ] unsuspendUser method implemented
- [ ] isUserSuspended method implemented
- [ ] Auth service checks suspension on login
- [ ] Tests exist for suspension logic
- [ ] Full TypeScript compilation passes
</verification>

<success_criteria>
1. No 'any' types in payments service (TYPE-03)
2. No 'any' types in subscriptions service (TYPE-04)
3. User suspension logic implemented (BUG-01)
4. Suspended users cannot log in
5. All TypeScript compilation passes
6. All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-services/02-03-SUMMARY.md`
</output>
