---
phase: 02-core-services
plan: 02-02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - backend/src/subscriptions/subscriptions.service.spec.ts
  - backend/src/webhooks/webhooks.service.ts
  - backend/src/webhooks/dto/webhook-events.dto.ts
  - backend/test/factories/subscription.factory.ts
autonomous: true
requirements:
  - TEST-05
  - TYPE-01
  - TYPE-02
must_haves:
  truths:
    - SubscriptionsService has 80%+ test coverage
    - Stripe webhook event types are properly defined
    - No 'any' types in webhook handling code
  artifacts:
    - path: backend/src/subscriptions/subscriptions.service.spec.ts
      provides: Comprehensive SubscriptionsService tests
    - path: backend/src/webhooks/dto/webhook-events.dto.ts
      provides: Typed Stripe webhook event interfaces
    - path: backend/test/factories/subscription.factory.ts
      provides: Subscription entity factory
  key_links:
    - from: backend/src/webhooks/dto/webhook-events.dto.ts
      to: backend/src/webhooks/webhooks.service.ts
      via: type imports
---

<objective>
Write comprehensive tests for SubscriptionsService and define proper TypeScript interfaces for Stripe webhook events, replacing all 'any' types.

Purpose: Ensure subscription management is reliable and webhook events are properly typed for type safety.
Output: Complete test suite for SubscriptionsService and typed webhook event definitions.
</objective>

<execution_context>
@/Users/kabo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kabo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kabo/Desktop/projects/stripe/.planning/ROADMAP.md
@/Users/kabo/Desktop/projects/stripe/.planning/REQUIREMENTS.md
@/Users/kabo/Desktop/projects/stripe/backend/src/subscriptions/subscriptions.service.ts
@/Users/kabo/Desktop/projects/stripe/backend/src/webhooks/webhooks.service.ts

## Current State
- SubscriptionsService exists but has no tests
- Webhook service uses 'any' types for Stripe events
- Need proper TypeScript interfaces for webhook events
- Testing infrastructure is ready

## Target Coverage
- 80%+ coverage for SubscriptionsService
- All webhook events properly typed
- No 'any' types remaining in webhooks module
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Stripe webhook event types</name>
  <files>backend/src/webhooks/dto/webhook-events.dto.ts</files>
  <action>
Create comprehensive TypeScript interfaces for Stripe webhook events.

1. Create backend/src/webhooks/dto/webhook-events.dto.ts:
   - Define StripeWebhookEvent base interface with:
     - id: string
     - object: 'event'
     - api_version: string
     - created: number
     - livemode: boolean
     - pending_webhooks: number
     - request: { id: string | null; idempotency_key: string | null }
     - type: string

2. Define specific event data interfaces:
   - PaymentIntentData with amount, currency, status, etc.
   - InvoiceData with id, amount_due, status, etc.
   - SubscriptionData with id, status, current_period_end, etc.
   - CustomerData with id, email, metadata, etc.

3. Define event type unions:
   - PaymentEvents: payment_intent.succeeded, payment_intent.payment_failed, etc.
   - InvoiceEvents: invoice.paid, invoice.payment_failed, etc.
   - SubscriptionEvents: subscription.created, subscription.updated, subscription.deleted, etc.
   - CustomerEvents: customer.created, customer.updated, customer.deleted

4. Create typed event handlers:
   - export type WebhookHandler<T> = (event: T) => Promise<void>
   - Create specific handler types for each event category

Reference Stripe API documentation for accurate field definitions.
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npx tsc --noEmit src/webhooks/dto/webhook-events.dto.ts</automated>
  </verify>
  <done>All webhook event types defined and compile without errors</done>
</task>

<task type="auto">
  <name>Task 2: Update webhooks service with proper types</name>
  <files>backend/src/webhooks/webhooks.service.ts</files>
  <action>
Update WebhooksService to use the new typed interfaces instead of 'any'.

1. Update imports in backend/src/webhooks/webhooks.service.ts:
   - Import typed event interfaces from webhook-events.dto
   - Remove any 'any' type usage

2. Update handleWebhook method:
   - Change signature to use typed events
   - Add type guards for event type discrimination
   - Ensure all event handlers use proper types

3. Update individual event handlers:
   - handlePaymentIntentSucceeded: use PaymentIntentSucceededEvent
   - handlePaymentIntentFailed: use PaymentIntentFailedEvent
   - handleInvoicePaid: use InvoicePaidEvent
   - handleSubscriptionUpdated: use SubscriptionUpdatedEvent
   - handleCustomerUpdated: use CustomerUpdatedEvent

4. Add runtime validation:
   - Validate event structure before processing
   - Log warnings for unrecognized event types
   - Return appropriate error responses for invalid events

5. Run TypeScript compiler to verify no 'any' types remain:
   - npx tsc --noEmit
   - Check for any type errors
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npx tsc --noEmit src/webhooks/webhooks.service.ts</automated>
  </verify>
  <done>Webhooks service compiles without 'any' types</done>
</task>

<task type="auto">
  <name>Task 3: Create subscription factory and write tests</name>
  <files>backend/test/factories/subscription.factory.ts, backend/src/subscriptions/subscriptions.service.spec.ts</files>
  <action>
Create subscription factory and comprehensive tests for SubscriptionsService.

1. Create backend/test/factories/subscription.factory.ts:
   - Export createSubscriptionFactory function
   - Include all Subscription entity fields: id, userId, stripeSubscriptionId, status, planId, currentPeriodStart, currentPeriodEnd, cancelAtPeriodEnd, createdAt, updatedAt
   - Allow overrides via partial object parameter
   - Create helper for different subscription statuses (active, canceled, past_due, etc.)

2. Create backend/src/subscriptions/subscriptions.service.spec.ts:
   - Set up test module with mocked dependencies
   - Test createSubscription:
     * Success: creates subscription with valid data
     * Error: handles Stripe API errors
     * Error: validates plan exists
   - Test cancelSubscription:
     * Success: cancels at period end
     * Success: immediate cancellation
     * Error: handles non-existent subscription
   - Test updateSubscription:
     * Success: upgrades plan
     * Success: changes quantity
     * Error: handles invalid plan changes
   - Test getSubscriptionStatus:
     * Returns correct status
     * Handles expired subscriptions
   - Test handleSubscriptionRenewal:
     * Processes successful renewal
     * Handles failed renewal payments

3. Ensure 80%+ coverage:
   - Run tests with coverage
   - Add tests for edge cases if needed
</action>
  <verify>
    <automated>cd /Users/kabo/Desktop/projects/stripe/backend && npm run test -- --run --coverage src/subscriptions/subscriptions.service.spec.ts</automated>
  </verify>
  <done>SubscriptionsService has 80%+ coverage with all types defined</done>
</task>

</tasks>

<verification>
- [ ] Webhook event types defined in webhook-events.dto.ts
- [ ] Webhooks service uses proper types (no 'any')
- [ ] Subscription factory creates valid entities
- [ ] createSubscription has tests
- [ ] cancelSubscription has tests
- [ ] updateSubscription has tests
- [ ] getSubscriptionStatus has tests
- [ ] Coverage report shows 80%+ for SubscriptionsService
</verification>

<success_criteria>
1. SubscriptionsService has 80%+ test coverage (TEST-05)
2. Stripe webhook types are properly defined (TYPE-01, TYPE-02)
3. No 'any' types remain in webhooks module
4. All webhook event handlers are properly typed
5. Tests run successfully with npm run test
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-services/02-02-SUMMARY.md`
</output>
